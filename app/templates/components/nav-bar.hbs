<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed"
      data-toggle="collapse" data-target="#menu-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{ link-to 'Alphanet CMS' 'index' class="navbar-brand" }}
    </div>

    <div class="collapse navbar-collapse" id="menu-collapse">
      <ul class="nav navbar-nav">
        {{#if session.isAuthenticated}}
        <li>{{ link-to 'Manage Tickets' 'ticket' }}</li>
        <li>{{ link-to 'Add Tickets' 'ticket.add' }}</li>
        <li>{{ link-to 'Customers' 'customers' }}</li>
      {{/if}}

      </ul>
      {{#if session.isAuthenticated}}
          <p class="navbar-right navbar-text">Welcome {{session.apiKey}} </p>
         <button class="navbar-right btn btn-default navbar-btn" {{action "logOut"}} > Logout </button> 
       {{/if}}
    </div>
  </div>
</nav>
{{yield}}
